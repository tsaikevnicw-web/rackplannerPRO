# RACK PLANNER PRO 機櫃規劃與拓撲管理系統 ── 使用手冊

本手冊旨在引導使用者（系統工程師、網路架構師及機房運維人員）快速熟悉並深入掌握 **RACK PLANNER PRO** 的核心操作與進階配置。本系統專為資料中心機櫃硬體擺放、伺服器規格自訂、水風冷散熱模擬、以及東西向（運算/AI）與南北向（管理/融合）雙 Fabric 網路拓撲之規劃與管理所設計。

---

## 目錄

- [一、 系統概述與核心架構](#一-系統概述與核心架構)
  - [1.1 系統定位](#11-系統定位)
  - [1.2 三大核心檢視視圖](#12-三大核心檢視視圖)
  - [1.3 全域資源統計列](#13-全域資源統計列)
- [二、 核心操作介面說明](#二-核心操作介面說明)
  - [2.1 頂部控制列 (Header)](#21-頂部控制列-header)
  - [2.2 左側設備清單 (Sidebar)](#22-左側設備清單-sidebar)
  - [2.3 中央視覺化畫布 (Canvas)](#23-中央視覺化畫布-canvas)
  - [2.4 右側設備屬性與設定面板 (RightPanel)](#24-右側設備屬性與設定面板-rightpanel)
- [三、 設備分類與規格配置指南](#三-設備分類與規格配置指南)
  - [3.1 伺服器分類與高度 (U 數) 自訂](#31-伺服器分類與高度-u-數-自訂)
  - [3.2 高階 PCIe 插槽動態配置](#32-高階-pcie-插槽動態配置)
  - [3.3 網路埠口與管理連線限制](#33-網路埠口與管理連線限制)
  - [3.4 液冷系統冷卻配置 (Cooling Configuration)](#34-液冷系統冷卻配置-cooling-configuration)
  - [3.5 側掛液冷單元 (SideCDU) 操作](#35-側掛液冷單元-sidecdu-操作)
- [四、 網路與水路拓撲管理](#四-網路與水路拓撲管理)
  - [4.1 物理埠口對接與拖曳連線規則](#41-物理埠口對接與拖曳連線規則)
  - [4.2 邏輯網路拓撲架構圖](#42-邏輯網路拓撲架構圖)
  - [4.3 連線視覺化色彩定義](#43-連線視覺化色彩定義)
  - [4.4 線路路徑與走線槽動態最佳化](#44-線路路徑與走線槽動態最佳化)
- [五、 進階快捷操作與範本應用](#五-進階快捷操作與範本應用)
  - [5.1 鍵盤快捷鍵 (Hotkeys)](#51-鍵盤快捷鍵-hotkeys)
  - [5.2 命名自動遞增邏輯](#52-命名自動遞增邏輯)
  - [5.3 RA 建議配置範本一鍵套用](#53-ra-建議配置範本一鍵套用)
- [六、 數據匯出與整合](#六-數據匯出與整合)
  - [6.1 物料清單 (BOM) 匯出](#61-物料清單-bom-匯出)
  - [6.2 網路線路表 (Cable Routing) 匯出](#62-網路線路表-cable-routing-匯出)
  - [6.3 畫布架構圖截圖存檔](#63-畫布架構圖截圖存檔)
- [七、 貨櫃式資料中心 (Container Data Center) 規畫指南](#七-貨櫃式資料中心-container-data-center-規畫指南)
  - [7.1 視圖與尺寸切換](#71-視圖與尺寸切換)
  - [7.2 基礎設施元件配置與拖放](#72-基礎設施元件配置與拖放)
  - [7.3 貨櫃級指標計算 (PUE 與總重負荷)](#73-貨櫃級指標計算-pue-與總重負荷)
  - [7.4 雙擊鑽取單櫃編輯](#74-雙擊鑽取單櫃編輯)

---

## 一、 系統概述與核心架構

### 1.1 系統定位
**RACK PLANNER PRO** 是一套專為下一代資料中心機房量身打造的機櫃規劃與線路架構設計工具。它打破了傳統試算表紀錄與 2D 圖紙規劃的限制，將**物理機櫃架構**、**實體埠口佈線**、**散熱冷卻配置**與**邏輯網路拓撲**整合在同一套互動式的網頁介面中，並支援動態規格計算與報表產出。

### 1.2 三大核心檢視視圖

系統提供三種檢視視圖，使用者可透過頂部控制列右側的切換開關進行即時切換，且資料保持即時同步：

1.  **單櫃檢視模式 (Single Rack)**
    *   **用途**：聚焦於單一機櫃內部的設備編排與細節微調。
    *   **特性**：提供詳細的物理位置（第 1U 到第 48U）刻度尺，並可在右側設定面板中針對機櫃名稱、機櫃類型（如 General 泛用或 ORv3）與總 U 數進行修改。
2.  **總覽檢視模式 (Rack Overview)**
    *   **用途**：跨機櫃排布與大格局物理架構視覺化。
    *   **特性**：將所有已建立的實體機櫃與側掛設備（SideCDU）以水平方式並排展示。支援**自適應縮放 (Fit to Screen)** 功能，不論建立多少台機櫃，皆能一目了然。
3.  **網路拓撲檢視模式 (Network Topology)**
    *   **用途**：呈現資料中心內的邏輯與實體網路連線關係。
    *   **特性**：系統自動解析設備角色，並將拓撲明確劃分為 **North-South Fabric（南北向）**、**East-West Fabric（東西向）** 與底部之 **Endpoint Layer（終端運算與儲存）** 三大區塊。

### 1.3 全域資源統計列
位於頂部控制列的左下方，提供對目前專案中所有已上架設備（排除 SideCDU）的實時運算數據：
*   **空間 (U)**：所有設備佔用的總機櫃高度空間。
*   **功耗 (W)**：加總所有伺服器、交換器、CDU 等設備設定之功耗。
*   **報價 (USD)**：加總目前上架的所有硬體設備之單價總額。

---

## 二、 核心操作介面說明

```mermaid
graph TD
    A[RACK PLANNER PRO 介面] --> B[頂部控制列 Header]
    A --> C[左側設備清單 Sidebar]
    A --> D[中央畫布 Canvas]
    A --> E[右側設定面板 RightPanel]
    
    B --> B1[專案匯入/匯出 JSON]
    B --> B2[報表匯出 CSV]
    B --> B3[視圖切換 單櫃/總覽/拓撲]
    B --> B4[RA 建議配置]
    
    C --> C1[伺服器分類]
    C --> C2[液冷 CDU]
    C --> C3[儲存與網路]
    
    D --> D1[實體 U 數刻度軌道]
    D --> D2[連接埠點對點拉線]
    D --> D3[側掛液冷 SideCDU]
    
    E --> E1[基本資訊與功耗報價]
    E --> E2[伺服器 U 數/Node 類型自訂]
    E E3[液冷水路設定]
    E --> E4[動態 PCIe 插槽與規格輸入]
```

### 2.1 頂部控制列 (Header)
*   **截圖存檔**：一鍵將中央畫布中的機櫃佈局匯出為 PNG 圖檔。系統會自動移除臨時編輯框線與套用深底色，並在圖片正中央印上 **Inventec Confidential** 機密戳記與當前系統時間戳記浮水印。
*   **檔案選單**：
    *   `讀取專案檔 (.json)`：載入舊有規畫。
    *   `儲存目前專案 (.json)`：將當前機櫃、設備屬性與全部連線狀態備份至本地。
    *   `匯出 BOM 表 (.csv)`：生成詳細的硬體材料清單（包含各節點規格與網路線材統計）。
    *   `匯出網路線路表 (.csv)`：生成所有網路交換器之線路對接紀錄（Cable Routing Sheet）。
*   **新增機櫃**：在畫布右側新增一台實體機櫃（預設高度 48U，命名如 RACK-002）。
*   **建議配置**：點選下拉選單，可以針對不同規模需求（20台、16台、4台、2台伺服器）快速載入並套用 Inventec RA 建議硬體配置。
*   **一鍵清除**：清空所有設備。在「單櫃模式」下會清空當前選取機櫃；在「總覽模式」下會重置整個專案。
*   **顯示/隱藏線路**：切換畫布上實體網路線與水冷管路路徑的顯示或隱藏。

### 2.2 左側設備清單 (Sidebar)
列出系統中可使用的標準硬體範本，按住並拖曳（Drag & Drop）至中央機櫃的特定 U 數即可上架：
*   **伺服器 (Server)**：
    *   `General Purpose`：標準通用型伺服器。
    *   `High Density`：高密度多節點伺服器。
    *   `AI Server`：人工智慧與 GPU 運算伺服器。
*   **CDU (冷卻分配單元)**：
    *   `4U In Rack CDU`：安裝於機櫃內部的液冷控制單元。
    *   `Liquid to Air CDU (SideCDU)`：側掛型液冷散熱機櫃（高度為 48U，佔用一個完整 Sidecar 空間）。
*   **磁碟陣列 (Storage)**：提供 `1U 儲存伺服器` 與 `2U 儲存伺服器`。
*   **網路設備 (Network)**：提供通用 `交換器 (Switch)` 與 `路由器 (Router)`。
*   **其他設備**：`不斷電系統 (UPS)` 與物理填充專用 `空白擋板 (Blank)`。

### 2.3 中央視覺化畫布 (Canvas)
*   **物理機櫃刻度**：模擬真實機櫃架構，附有金屬掛耳、實體固定螺絲孔與散熱排柵。每一格 U 數皆提供對應滑鼠拖入與釋放（Drop）的定位引導。
*   **連線埠點**：設備右側排布著綠色（已連線）或灰色（未連線）的實體連接埠，使用者可直接點選埠點並拉出線路，對接至其他設備之連接埠。
*   **側掛區**：若上架 `Liquid to Air CDU` (SideCDU)，會自動被吸附在主機櫃的右側（獨立寬度 210px），並顯示動態冷卻風扇與底部水冷迴路進出水排埠。

### 2.4 右側設備屬性與設定面板 (RightPanel)
當選取機櫃或任一設備時，此面板會自動滑出，提供進階的參數微調：
*   **主機資訊**：可在此編輯設備自訂名稱（如 Server-01）、拓撲群組、功耗（W）、設備報價（USD）。
*   **機櫃設定**：編輯機櫃名稱、機櫃規格（標準 General 或 開放運算計畫 ORv3）。
*   **動態規格輸入**：針對伺服器種類的不同，右側面板會提供動態配置功能（詳見第三章）。

---

## 三、 設備分類與規格配置指南

本系統針對伺服器提供了高度靈活的參數自訂機制。

### 3.1 伺服器分類與高度 (U 數) 自訂
在選取設備後，於右側設定面板的 **伺服器設定 (Server Config)** 下拉選單中，可為三種伺服器自訂高度，中央畫布中的設備將隨之動態增高或縮小，並同步釋放或佔用機櫃的物理 U 數空間：

| 伺服器分類 | 支援高度與節點選項 | 說明 |
| :--- | :--- | :--- |
| **General Purpose** | 1U, 2U, 3U, 4U | 適用於標準 1~4U 通用伺服器。 |
| **High Density** | 1U1N, 1U2N, 2U1N, 2U2N, 2U4N | U數代表高度，N數代表該物理主機內部包含的獨立運算節點（Nodes）數量。 |
| **AI Server** | 4U, 5U, 6U, 10U | 適用於大型高功耗 GPU 加速運算伺服器。 |

> [!NOTE]
> *   在畫布中，**High Density 1U2N** 伺服器的 Node 1 與 Node 2 採**一列水平排列**呈現以節省空間。
> *   **High Density 2U4N** 伺服器的 Node 1 到 Node 4 則會以 **2x2（二行二列）** 的網格方塊呈現。
> *   在網路拓撲中，為了美觀與排版一致性，**2U4N 伺服器之節點會以二列（Node 1 & 2 / Node 3 & 4）排列**，而不會佔據四列，確保圖面整潔。

---

### 3.2 高階 PCIe 插槽動態配置
為了因應複雜的硬體規格紀錄，在 General Purpose 與 AI Server 的硬體規格區中，提供 **PCIe Slots 數量選單（可選擇 1 ~ 13 個）**：
1.  當使用者從下拉選單選擇 PCIe 插槽數量（例如選 4）時，右側設定面板會**動態生成 4 個自訂欄位**，依序標示為 `PCIe Slot 1` 到 `PCIe Slot 4`。
2.  使用者可針對每個插槽填入該張 PCIe 介面卡的**零組件名稱 (Model)** 與**連接埠數量 (Qty)**（即該卡有幾個實體網孔）。
3.  填入連接埠數量後，中央畫布及邏輯拓撲圖中的該設備右側，將**即時長出對應數量的連線埠點**，供使用者進行點對點佈線。

> [!TIP]
> **High Density 伺服器** 亦支援此項動態配置！由於高密度伺服器由多個獨立節點組成，設定面板中會為每個 Node（Node 1、Node 2...）獨立提供 `PCIe Slots (1~13)` 下拉選單。使用者可為不同 Node 配置完全不同的 PCIe 擴充卡規格。

---

### 3.3 網路埠口與管理連線限制
*   **Super NIC Mgt 限制**：
    在 **General Purpose 伺服器**中，管理網路 Super NIC Mgt 的數量上限與硬體規格中配置的 PCIe Slots 數量維持一致。例如：若 PCIe Slots 選擇 4，則 Super NIC Mgt 埠數最大僅能設定為 4，避免超出物理插槽上限。
*   **AI Server 的 EW NIC 配置**：
    *   在 AI 伺服器的「網路連線狀態與數量」區塊中，**EW NIC 數量開放為可編輯狀態**。
    *   使用者可以直接修改其數量（預設為 8），畫布與拓撲圖中的 `EW NIC` 錨點將隨之動態增減。
    *   **EW NIC Network Type** 可切換為：
        *   `Ethernet / RoCE v2`（採用**綠線**連線，使用 MMA4Z00 光模組與 MFP7E10 線材）。
        *   `InfiniBand / NDR`（採用**橘線**連線，使用 MCA7K10 一體化光纖線材）。

---

### 3.4 液冷系統冷卻配置 (Cooling Configuration)
當設備類型為伺服器、儲存裝置或 CDU 時，右側面板會提供液冷配置區：
*   **Host Cooling (主機冷卻)**：可切換為 `AC (風冷)` 或 `LC (液冷)`。
*   **GPU Cooling (GPU 冷卻，僅 AI Server 顯示)**：可切換為 `AC` 或 `LC`。

> [!IMPORTANT]
> 一旦將 Host 或 GPU 調整為 **LC (Liquid Cooling)**，該伺服器在畫布上會**動態啟用水冷接頭錨點**：
> *   **Host LC** 啟用：產生 `host_water_cold` (C) 與 `host_water_hot` (H) 埠點。
> *   **GPU LC** 啟用：產生 `water_cold` (C) 與 `water_hot` (H) 埠點。

---

### 3.5 側掛液冷單元 (SideCDU) 操作
SideCDU（寬度 210U，高度與機櫃齊平）主要用以接收整櫃伺服器產生的廢熱。
1.  當伺服器啟用水冷錨點後，使用者可使用滑鼠將伺服器上的 `C (冷水)` / `H (熱水)` 連接點拖曳拉線至右側 SideCDU 下方的 **Water Loop - Cold** 與 **Water Loop - Hot** 排埠上。
2.  藉此，系統即可在線路表中自動判定水路拓撲。

---

## 四、 網路與水路拓撲管理

### 4.1 物理埠口對接與拖曳連線規則
不論是在「物理機櫃畫布」或「邏輯網路拓撲圖」中，皆可進行動態連線：
*   **建立連線**：以滑鼠左鍵點選任一設備之實體埠口（如 Switch 的 Port 1 或伺服器的 PCIe Slot 1 P1），按住並拖曳拉出一條虛線，移動至目標設備的埠口放開滑鼠，即可建立連線。
*   **中斷連線**：在任一已連線的埠點（顯示綠色或特定色）上**連點滑鼠左鍵兩次 (Double Click)**，即可切斷與該埠口對接的所有線路。
*   **多對一連線功能 (Pcie Slot / NIC)**：
    伺服器或儲存設備之單一網口支援多對一（一埠接至多個交換器埠）連線，最多可同時連接 8 條線路。此時埠點中心會以黑色數字標示目前連線數（例如 `3`）。

---

### 4.2 邏輯網路拓撲架構圖
進入「網路拓撲」視圖，系統會解析所有設備的 `fabricGroup`（網路用途）與 `networkRole`（網路角色，分為 Spine 與 Leaf）：

```
+-------------------------------------------------------------+
|                     SPINE LAYER (骨幹層)                     |
|                                                             |
|   [NS-Spine: 核心管理交換器]     [EW-Spine: 核心東西向交換器]   |
+-------------------------------------------------------------+
                              |
+-------------------------------------------------------------+
|                     LEAF LAYER (存取層)                      |
|                                                             |
|   [NS-Leaf: 機架頂端交換器]       [EW-Leaf: 東西向 Leaf 交換器]  |
+-------------------------------------------------------------+
                              |
+-------------------------------------------------------------+
|                    ENDPOINT LAYER (設備層)                  |
|                                                             |
|    RACK-001 (5台伺服器)            RACK-002 (4台伺服器)      |
|    +------------------+           +------------------+      |
|    | ServerGeneral-01 |           | ServerGeneral-02 |      |
|    | ServerAI-01      |           | ServerAI-02      |      |
|    +------------------+           +------------------+      |
+-------------------------------------------------------------+
```

*   **自動分組 (Topology Group)**：
    Endpoint 設備會依照 `topologyGroup`（預設為所在實體機櫃名稱，如 RACK-001）進行分組外框繪製。
*   **分組收合與一鍵展開**：
    *   點擊分組標題旁的展開/收合圖示，可將整個 Group 折疊為一個緊湊的小方塊（顯示如 "RACK-001 - 5 Devices"）。這時該 Group 所有的對外連線會自動匯總為一條較粗的**群組匯總連線**。
    *   點擊分組外框右上角的 `Maximize2` 圖示，或點擊 Fabric 標題旁的 **「一鍵收起 / 一鍵展開」** 鍵，可批次展開或折疊所有群組，適合處理龐大網路。
*   **等寬設計 (Symmetrical Group Size)**：
    Endpoint Layer 的每一個分組寬度固定為 **420px**，確保不論該機櫃內是擺放寬度較寬的 High Density (2U4N) 或是 AI 伺服器，整個拓撲圖的 Group 大小皆維持對稱與整潔。

---

### 4.3 連線視覺化色彩定義
線路在畫布與拓撲上，會依據其**功能與連接端點類型**動態顯示特定顏色，便於維運辨識：

| 線路顏色 | 代表之線路類型/用途 |
| :--- | :--- |
| **藍色 (`#60a5fa`)** | BMC 管理網路線 / Switch 1G 網路線 / 水冷冷水管 (`water_cold`) |
| **紅色 (`#ef4444` / `#f87171`)** | Router 幹線 / 水冷熱水管 (`water_hot`) / High Density 節點 2 連線 |
| **綠色 (`#22c55e` / `#34d399`)** | AI 伺服器東西向 RoCE v2 運算網路 (Ethernet) / Switch 800G 線路 |
| **橘色 (`#fb923c` / `#f97316`)** | AI 伺服器東西向 NDR 運算網路 (InfiniBand) / Switch 10G 線路 |
| **黃色 (`#facc15`)** | Switch 400G 線路 / 通用 PCIe Slot 連線 / High Density 節點 1 連線 |
| **紫色 (`#a855f7`)** | 通用交換器 Port-to-Port 幹線 / Switch 傳輸線 |
| **彩虹色 (Rainbow)** | 當拓撲群組（Group）收合時，群組間的匯總連線會依分組索引帶入彩虹色。 |

---

### 4.4 線路路徑與走線槽動態最佳化

為了模擬真實資料中心的佈線工程，系統提供「最佳化走線」模式。此模式會將實體畫布上的混亂交叉線路自動收納入側邊走線槽中：

1. **走線路徑規則**：
   * **同機櫃內連線**：線路從埠點水平拉出，直接進入機櫃兩側的 32px 「立柱理線槽 (Pillars)」內。如果兩個對接埠口位於同側，則直接在該側槽道內垂直爬升；若為對角埠口，線路會沿著槽道爬到機櫃頂部 (U48) 或底部越過，不阻擋伺服器面板。
   * **跨機櫃連線**：線路由來源埠口進入立柱槽後，一路爬升至機櫃上方，透過架空在機櫃頂端的「架空橋接線槽 (Overhead Tray)」橫向跨越至目標機櫃，再由其立柱槽垂直下降，水平插入目標埠口。
   * **暫存拖曳線**：拖曳進行拉線連接時，未連接的線路仍會呈貝氏曲線，一旦放開滑鼠成功連線，即刻自動套用最佳化收納。
2. **模式切換**：
   * 點選頂部控制列的 **「最佳化走線」** / **「直連走線」** 按鈕可切換模式。
   * **最佳化走線**（預設啟用）：呈現整齊乾淨、防交叉阻擋的真實走線，適合生成施工圖。
   * **直連走線**：採用原本的弧形貝氏曲線，適合快速釐清端點關聯。
   * *註：在「網路拓撲」視圖下，線路一律採用邏輯貝氏曲線以維持版面簡潔。*

---

## 五、 進階快捷操作與範本應用

### 5.1 鍵盤快捷鍵 (Hotkeys)
在畫布中未輸入任何表單文字時，可使用以下鍵盤快捷鍵加速部署：
*   **Delete 鍵**：快速刪除選取的設備（無需確認視窗）。對應的連線埠與線路會自動被安全拆除。
*   **Ctrl + C (或 Meta + C)**：複製選取的設備，亦或是複製選取的**整個物理機櫃（含機櫃內所有上架伺服器與設定）**。
*   **Ctrl + V (或 Meta + V)**：將複製的設備貼上至選定機櫃內第一個有空位的 U 數；若複製的是機櫃，則會直接在最右側貼上一台全新且包含相同內部設備配置的機櫃。

### 5.2 命名自動遞增邏輯
當使用貼上（Ctrl+V）或複製設備時，系統會自動分析來源名稱末端的數字。若名稱以 `-01`、`-001` 或 `_1` 等數字結尾，貼上後的新設備/新機櫃名稱會**自動尋找專案內最大序號並遞增**。
> **範例**：複製 `RACK-001` 並貼上，新機櫃將自動命名為 `RACK-002`，其內部之 `Server-01` 將被自動改名為 `Server-02`，避免名稱衝突。

### 5.3 RA 建議配置範本一鍵套用
頂部控制列的 **「建議配置」** 功能提供了 Inventec 生產線與系統設計常用的四種標準規格模組。點擊即可直接載入：
*   **20台 範本**：自動建立含 20 台 AI Server/High Density 的高規格大型機櫃叢集與 800G Spine-Leaf 交換器。
*   **16台 範本**：配置 16 台通用伺服器與雙 Fabric 網路對接。
*   **4台 / 2台 範本**：輕量級測試範本，適合快速體驗線路互連與水冷迴路串接。

---

## 六、 數據匯出與整合

### 6.1 物料清單 (BOM) 匯出
點選 `檔案` -> `匯出 BOM 表 (.csv)` 將產出一份 UTF-8 (帶 BOM) 的 CSV 報表，欄位解析如下：

1.  **實體設備清單**：
    羅列每一台設備的所在機櫃名稱、機架位置（U數範圍）、設備類型、功耗、解熱能力（CDU用）與報價。
2.  **動態硬體規格分解**：
    *   將選取的零組件 `CPU`、`DIMM`、`GPU`、`M.2`、`HDD`、`PSU` 等型號與數量正規化輸出（例如 `"Intel Xeon 64C *2"`）。
    *   `PCIe Slot 1` 與 `PCIe Slot 2` 的網卡數量將以獨立欄位輸出。
    *   `PCIe Slot 3` 至 `Slot 13`，以及 High Density 各個子節點中 Slot 3 以上的規格，會自動以分號連接，格式化歸類在最後一欄 **Other (型號*數量)** 欄位中，保持報表整潔。
3.  **網路線材與光模組自動統計**：
    在 BOM 表的最下方，系統會遍歷全專案的連線狀態，自動估算並輸出所需要的耗材總量：
    *   1G/10G/100G/400G/800G 線材數量。
    *   QSFP56 / 2x400G 光收發模組 (Transceiver) 數量。
    *   AI Server 若使用 Ethernet，會列出 `MMA4Z00-NS-FLT` (NIC端) 與 `MMA4Z00-NS` (SW端) 光模組及 `MFP7E10` 線材。
    *   AI Server 若使用 InfiniBand，會折算 `MCA7K10` 一體化主動式光纜。

### 6.2 網路線路表 (Cable Routing) 匯出
點選 `檔案` -> `匯出網路線路表 (.csv)` 會將專案中所有連線以「網路交換器」為核心進行整理：
*   **排序機制**：自動依照網路用途 (Fabric Group) -> 網路角色 (Spine/Leaf) -> 交換器名稱排序。
*   **每一埠對接狀態**：詳列交換器上的 `Port 1` ~ `Port N` 實體埠口連接到了哪一台機櫃、哪一台伺服器的哪一個 PCIe Slot 或 OCP/BMC。
*   **光模組與線材規格**：若對接端為 AI 伺服器，會自動依據其網路傳輸技術（Ethernet / InfiniBand），在欄位中自動帶出建議的 Transceiver (NIC)、Transceiver (SW) 型號與 Cable 型號與用量，方便佈線工人按表施工。

### 6.3 畫布架構圖截圖存檔
點選頂部的 **「截圖存檔」**，系統將以超高解析度渲染目前的畫布內容，並在背景加上帶有當前時間的 **Inventec Confidential** 斜角浮水印。此圖檔可直接用於客戶簡報（PPT）或系統架構設計書中，展現極高專業度。

---

## 七、 貨櫃式資料中心 (Container Data Center) 規畫指南

### 7.1 視圖與尺寸切換
點擊頂部導覽列視圖切換的 **「貨櫃佈局」** 按鈕，系統將切換至貨櫃式資料中心配置模式。在該模式下，您可以藉由頂部控制面板：
*   切換 **20呎貨櫃** (提供 10 個基礎插槽網格) 或 **40呎貨櫃** (提供 20 個基礎插槽網格)。
*   即時預覽貨櫃的俯視網格，格位寬度符合標準的 600mm 模組化機櫃尺寸。

### 7.2 基礎設施元件配置與拖放
當處於貨櫃佈局模式時，左側設備清單會自動切換為 **「貨櫃基礎元件」**：
*   **IT 機櫃 (General / ORv3)**：用於裝載運算與儲存設備。
*   **列間空調 (In-Row Cooling)**：提供列間散熱解熱（預設 75kW，本體功耗 4500W）。
*   **UPS 動力總櫃**：負責不斷電應急供電（預設容量 120kW）。
*   **鋰電池機櫃**：儲備電能（預設容量 100kWh，本體重量 1200kg）。
*   **低壓配電總櫃 (Switchboard)**：電網配電總開關控制。
您可以直接將這些基礎設施元件拖入貨櫃網格中，或是拖動已放置的機櫃進行插槽位置交換。

### 7.3 貨櫃級指標計算 (PUE 與總重負荷)
頂部的數據看板會動態彙整貨櫃內所有已放置機櫃的資源消耗與總重：
*   **PUE 估算 (Power Usage Effectiveness)**：根據 `(IT總功耗 + 空調功耗 + UPS損耗) / IT總功耗` 進行動態推估（基本 PUE 1.15）。藉由合理分配 In-Row 空調位置與 IT 功耗，可優化綠能效益。
*   **總重量 (Total Weight) 與安全限載警告**：貨櫃總重量若超出安全起吊與物流運輸上限 **30,000 kg (30 噸)**，數值將以紅色閃爍提示，避免實務吊裝上的物流安全風險。
*   **總配電負載**：顯示總功耗相對於貨櫃供電容量上限 (500 kW) 的比例。

### 7.4 雙擊鑽取單櫃編輯
在貨櫃佈局中，**雙擊任意 IT 機櫃 (General 或 ORv3)**，系統將無縫進入該機櫃的「單櫃編輯」模式。在此模式下，您可以如同以往地在該機架內放置並連接伺服器、交換器等 IT 設備。完成細部配置後，再點選頂部「貨櫃佈局」即可一鍵切回貨櫃層級，此時該 IT 機櫃所屬的伺服器功耗、設備重量將會自動連動並加總至貨櫃層級統計指標中。

---
*本系統由 英業達集團 (Inventec Corp.) 維護。*
*如有系統疑問或客製需求，請洽 Kevin Tsai (Tsai.KevinC.W@inventec.com)。*
